node_modules
dist
uploads
.env
.env.*
!.env.example

# Etapa 15M.4: snapshots locales pre-repair de scripts/reconcile-normal-hours.ts
# (backup lógico para rollback manual, nunca versionado -- ver
# docs/decisions/ATTENDANCE_NORMAL_HOURS_RECONCILIATION_15M4.md §17).
.reconciliation-snapshots/
